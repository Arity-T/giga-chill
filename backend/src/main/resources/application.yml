spring:
  application:
    name: gigachill
  datasource:
    url: jdbc:postgresql://localhost:5432/gigachill
    username: postgres
    password: postgres

server:
  port: 3000

jwt:
  expiration: 1d

frontend:
  origin: "*"
