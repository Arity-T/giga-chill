# Файл ограничивает контекст при сборке образа для настройки MinIO (см. Dockerfile)

# Исключаем все файлы, чтобы не раздувать образ
*

# Кроме необходимых для сборки
!bootstrap_minio.sh
!policies/**
!ilm/**